// demo is the code of subclass within the same package that is why default member is accessible.+
//subclass is a child class that inherits(properties) from the other casss or the parent class.
// class being inherited is called superclass or parent class.
// class that inherits is called subclass or child class.
//subclass is a class that extends the another class and inherits non private members of the parent class.
package demo;

public class Parent {
    private int a = 10;
    int b = 20;
    protected int c =30;
    public int d = 40;
}
